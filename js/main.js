jQuery(document).ready(function($){
	$('#slider').bjqs({
		width : 620,
		height : 310,
	})
	


});


document.addEventListener('DOMContentLoaded', function() {
  var hotelsearch = new Typed('#hotelsearch', {
    strings: ['City or Country '],
    attr: 'placeholder',
    typeSpeed: 80,
    backSpeed: 0,
    fadeOut: true,
    loop: true,
  });
});
